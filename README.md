# Book Manager Frontend

## Setup
1. Run `npm install`
2. Copy `.env.example` to `.env`
3. Set `VITE_API_URL=http://localhost:5000/api`
4. Run `npm run dev`

For deployment, change `VITE_API_URL` to your deployed backend URL followed by `/api`.
